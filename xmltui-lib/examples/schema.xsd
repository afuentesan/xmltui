<?xml version="1.0" encoding="UTF-8"?>
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" elementFormDefault="qualified">

    <!-- ========================================== -->
    <!-- TIPOS PERSONALIZADOS Y RESTRICCIONES       -->
    <!-- ========================================== -->
    
    <!-- Valida el atributo ratio: dos enteros sin signo separados por coma -->
    <xs:simpleType name="ratioType">
        <xs:restriction base="xs:string">
            <!-- Solo números sin decimales, separados por una coma y opcionalmente espacios -->
            <xs:pattern value="\d+,\s*\d+"/>
        </xs:restriction>
    </xs:simpleType>

    <!-- Restricción para los valores del atributo align -->
    <xs:simpleType name="alignType">
        <xs:restriction base="xs:string">
            <xs:enumeration value="left"/>
            <xs:enumeration value="right"/>
            <xs:enumeration value="center"/>
        </xs:restriction>
    </xs:simpleType>

    <!-- Restricción para los valores del atributo dir -->
    <xs:simpleType name="dirType">
        <xs:restriction base="xs:string">
            <xs:enumeration value="horizontal"/>
            <xs:enumeration value="vertical"/>
        </xs:restriction>
    </xs:simpleType>

    <!-- Atributos que comparten TODOS los nodos dentro de body (y el propio body) -->
    <xs:attributeGroup name="commonBodyAttributes">
        <xs:attribute name="id" type="xs:string"/>
        <xs:attribute name="class" type="xs:string"/>
        <!-- Se usa xs:unsignedInt para garantizar que sean enteros positivos o cero -->
        <xs:attribute name="fill" type="xs:unsignedInt"/>
        <xs:attribute name="percentage" type="xs:unsignedInt"/>
        <xs:attribute name="min" type="xs:unsignedInt"/>
        <xs:attribute name="max" type="xs:unsignedInt"/>
        <xs:attribute name="length" type="xs:unsignedInt"/>
        <xs:attribute name="ratio" type="ratioType"/>
        <!-- Permite los atributos data-* -->
        <xs:anyAttribute namespace="##any" processContents="skip"/>
    </xs:attributeGroup>

    <!-- Todos los elementos posibles que pueden anidarse dentro de body o de un layout -->
    <xs:group name="bodyElements">
        <xs:choice>
            <xs:element ref="layout"/>
            <xs:element ref="line"/>
            <xs:element ref="span"/>
            <xs:element ref="input"/>
            <xs:element ref="a"/>
            <xs:element ref="button"/>
            <xs:element ref="command"/>
        </xs:choice>
    </xs:group>

    <!-- ========================================== -->
    <!-- ELEMENTOS DEL BODY                         -->
    <!-- ========================================== -->
    <xs:element name="layout">
        <xs:complexType>
            <xs:group ref="bodyElements" minOccurs="0" maxOccurs="unbounded"/>
            <xs:attribute name="dir" type="dirType"/>
            <xs:attributeGroup ref="commonBodyAttributes"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="line">
        <xs:complexType mixed="true">
            <xs:sequence>
                <xs:element ref="span" minOccurs="0" maxOccurs="unbounded"/>
            </xs:sequence>
            <xs:attribute name="align" type="alignType"/>
            <xs:attributeGroup ref="commonBodyAttributes"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="span">
        <xs:complexType mixed="true">
            <xs:attributeGroup ref="commonBodyAttributes"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="input">
        <xs:complexType>
            <xs:attribute name="type" type="xs:string"/>
            <xs:attribute name="value" type="xs:string"/>
            <xs:attribute name="align" type="alignType"/>
            <xs:attributeGroup ref="commonBodyAttributes"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="a">
        <xs:complexType mixed="true">
            <xs:attribute name="src" type="xs:string"/>
            <xs:attribute name="align" type="alignType"/>
            <xs:attributeGroup ref="commonBodyAttributes"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="button">
        <xs:complexType mixed="true">
            <xs:attribute name="enter" type="xs:string"/>
            <xs:attribute name="align" type="alignType"/>
            <xs:attribute name="enter-replace-childs" type="xs:string"/>
            <xs:attribute name="enter-replace-node" type="xs:string"/>
            <xs:attribute name="enter-change-value" type="xs:string"/>
            <xs:attribute name="enter-data" type="xs:string"/>
            <xs:attributeGroup ref="commonBodyAttributes"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="command">
        <xs:complexType>
            <xs:attribute name="exec" type="xs:string"/>
            <xs:attribute name="child-type" type="xs:string"/>
            <!-- He aplicado los mismos tipos a los hijos del command para mantener coherencia -->
            <xs:attribute name="child-align" type="alignType"/>
            <xs:attribute name="child-dir" type="dirType"/>
            <xs:attributeGroup ref="commonBodyAttributes"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="body">
        <xs:complexType>
            <xs:group ref="bodyElements" minOccurs="0" maxOccurs="unbounded"/>
            <xs:attribute name="dir" type="dirType"/>
            <xs:attributeGroup ref="commonBodyAttributes"/>
        </xs:complexType>
    </xs:element>

    <!-- ========================================== -->
    <!-- ELEMENTOS DEL HEAD                         -->
    <!-- ========================================== -->
    <xs:element name="style">
        <xs:complexType mixed="true">
            <xs:attribute name="src" type="xs:string"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="env">
        <xs:complexType mixed="true">
            <xs:attribute name="name" type="xs:string" use="required"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="env-data">
        <xs:complexType>
            <xs:attribute name="name" type="xs:string" use="required"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="arg" type="xs:string"/>

    <xs:element name="arg-data">
        <xs:complexType>
            <xs:attribute name="name" type="xs:string" use="required"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="envs">
        <xs:complexType>
            <xs:choice minOccurs="0" maxOccurs="unbounded">
                <xs:element ref="env"/>
                <xs:element ref="env-data"/>
            </xs:choice>
        </xs:complexType>
    </xs:element>

    <xs:element name="args">
        <xs:complexType>
            <xs:choice minOccurs="0" maxOccurs="unbounded">
                <xs:element ref="arg"/>
                <xs:element ref="arg-data"/>
            </xs:choice>
        </xs:complexType>
    </xs:element>

    <xs:element name="code">
        <xs:complexType>
            <xs:sequence minOccurs="0">
                <xs:element ref="envs" minOccurs="0"/>
                <xs:element ref="args" minOccurs="0"/>
            </xs:sequence>
            <xs:attribute name="name" type="xs:string"/>
            <xs:attribute name="command" type="xs:string"/>
            <xs:attribute name="src" type="xs:string"/>
        </xs:complexType>
    </xs:element>

    <xs:element name="head">
        <xs:complexType>
            <xs:choice minOccurs="0" maxOccurs="unbounded">
                <xs:element ref="style"/>
                <xs:element ref="code"/>
            </xs:choice>
        </xs:complexType>
    </xs:element>

    <!-- ========================================== -->
    <!-- ELEMENTO RAÍZ                              -->
    <!-- ========================================== -->
    <xs:element name="rtml">
        <xs:complexType>
            <xs:sequence>
                <!-- minOccurs="0" hace que el head sea opcional -->
                <xs:element ref="head" minOccurs="0"/>
                <xs:element ref="body"/>
            </xs:sequence>
        </xs:complexType>
    </xs:element>

</xs:schema>